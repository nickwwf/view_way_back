# coding: utf-8


def is_in_scope(scope, endpoint):
    # For now, we'll just return True for all requests
    # In a real application, you would check if the user's scope allows access to the endpoint
    return True